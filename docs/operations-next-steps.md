# Operations implementation next steps

This document tracks the next safe package for the operations and content system.

- Keep the Operations home as the daily command center.
- Keep Smart Archive as a separate system.
- Add content registry runtime models in small steps.
- Connect content items to campaign links only after the registry is stable.

import type{ImpactRegion}from"@/types/impact";export function ImpactRegions({regions}:{regions:ImpactRegion[]}){return <section className="impact-section"><div className="site-container"><header className="section-intro"><span>حسب المنطقة</span><h2>أثر مرتبط بالمكان والمشروع</h2></header><div className="impact-region-rail">{regions.map(r=><article key={r.id}><div className="region-visual" aria-hidden="true"/><span>{r.label}</span><h3>{r.impactType}</h3><p>{r.status==="data-required"?"DATA REQUIRED":`${r.projectIds.length} مشاريع مرتبطة`}</p><small>{r.latestUpdate}</small><a href={`#records-${r.id}`}>عرض السجلات</a></article>)}</div></div></section>}

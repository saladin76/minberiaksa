"use client";import{useEffect,useRef,useState}from"react";import type{ImpactDocument}from"@/types/impact";const focusable='button:not([disabled]),[href],[tabindex]:not([tabindex="-1"])';export function ImpactDocuments({documents}:{documents:ImpactDocument[]}){const[selected,setSelected]=useState<ImpactDocument|null>(null);const dialog=useRef<HTMLDivElement>(null);const trigger=useRef<HTMLButtonElement|null>(null);useEffect(()=>{if(!selected||!dialog.current)return;const old=document.body.style.overflow;document.body.style.overflow="hidden";const nodes=()=>Array.from(dialog.current!.querySelectorAll<HTMLElement>(focusable));requestAnimationFrame(()=>nodes()[0]?.focus());const key=(e:KeyboardEvent)=>{if(e.key==="Escape")setSelected(null);if(e.key!=="Tab")return;const n=nodes();if(!n.length)return;const first=n[0],last=n[n.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}};document.addEventListener("keydown",key);return()=>{document.body.style.overflow=old;document.removeEventListener("keydown",key);trigger.current?.focus()}},[selected]);return <section id="documents" className="impact-section impact-documents"><div className="site-container"><header className="section-intro"><span>الوثيقة كأثر</span><h2>تقارير وصور وإثباتات مرتبطة بالمشروع</h2></header><div className="document-artifact-layout"><button className="featured-document" ref={trigger} type="button" onClick={()=>setSelected(documents[0])}><strong>{documents[0]?.title}</strong><span>{documents[0]?.note}</span><small>فتح المعاينة</small></button><div>{documents.slice(1).map(d=><button key={d.id} type="button" onClick={e=>{trigger.current=e.currentTarget;setSelected(d)}}><strong>{d.title}</strong><span>{d.note}</span></button>)}</div></div></div>{selected?<div className="impact-dialog-layer"><button className="dialog-backdrop" aria-label="إغلاق" onClick={()=>setSelected(null)}/><div ref={dialog} className="impact-dialog" role="dialog" aria-modal="true" aria-labelledby="impact-document-title"><header><h2 id="impact-document-title">{selected.title}</h2><button type="button" onClick={()=>setSelected(null)}>إغلاق</button></header><div className="document-preview-placeholder"><strong>{selected.note??"OFFICIAL FILE REQUIRED"}</strong><p>هذه معاينة Prototype فقط. لا يوجد PDF أو تنزيل حقيقي.</p></div><button type="button" disabled>تنزيل الملف — غير متاح</button></div></div>:null}</section>}

"use client";
import{useMemo,useState}from"react";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";import{useBasket}from"@/components/basket/use-basket";import{calculateBasketTotals,getBasketDocuments,getFrequencyLabel,getIntentionCounts}from"@/components/basket/basket-utils";import{paymentMethods}from"@/data/checkout";
export function SuccessExperience(){const router=useRouter(),{items,hydrated,checkoutSnapshot,clearBasket}=useBasket();const[message,setMessage]=useState("");const totals=useMemo(()=>calculateBasketTotals(items),[items]),documents=useMemo(()=>getBasketDocuments(items),[items]),intentions=useMemo(()=>getIntentionCounts(items),[items]);const payment=paymentMethods.find(m=>m.id===checkoutSnapshot?.paymentMethod)?.label??"طريقة تجريبية غير محددة";if(!hydrated)return <div role="status">جارٍ تحميل ملخص المعاينة…</div>;return <main id="main-content" className="result-page"><section><div className="result-banner">Prototype Success Preview · لم تتم معالجة أي دفعة حقيقية</div><h1>اكتملت معاينة عطائك بنجاح</h1><p>في النسخة التشغيلية ستظهر هنا نتيجة الدفع والإيصالات والشهادات وخيارات متابعة الأثر.</p><code>PREVIEW-DONATION-0001</code></section><section className="result-summary"><h2>ملخص النتيجة</h2><dl><div><dt>المبلغ لمرة واحدة</dt><dd>{totals.amountDueNow.toLocaleString("en-US")} USD</dd></div><div><dt>نيات التبرع</dt><dd>{Object.keys(intentions).length}</dd></div><div><dt>خطط مستمرة</dt><dd>{totals.recurringPlans.length}</dd></div><div><dt>وسيلة الدفع</dt><dd>{payment}</dd></div><div><dt>الحالة</dt><dd>Prototype only</dd></div></dl>{totals.recurringPlans.map(p=><p key={p.id}>{p.amount} USD {getFrequencyLabel(p.frequency)} · {p.projectTitle}</p>)}</section><section className="result-documents"><h2>الوثائق التي ستصدر</h2><div className="result-doc-grid">{documents.map(d=><article key={d.id}><h3>{d.title}</h3><p>سيُنشأ بعد الدفع الحقيقي</p><button type="button" onClick={()=>setMessage(`تم فتح معاينة ${d.title} دون إنشاء ملف.`)}>معاينة</button><button type="button" disabled>Download</button></article>)}</div></section><section className="result-actions"><Button type="button" onClick={()=>setMessage("ستظهر لوحة أثر المتبرع في مرحلة مستقلة.")}>معاينة أثر عطائك</Button><Button href="/projects" variant="outline">استكشف مشروعًا آخر</Button><Button href="/" variant="text">العودة للرئيسية</Button><button type="button" onClick={()=>setMessage("المشاركة لن تتضمن مبلغًا أو أسماء أو بيانات شخصية.")}>شارك أنك ساهمت في الخير — Prototype</button></section>{message&&<p role="status">{message}</p>}<section className="result-actions"><h2>إنهاء المعاينة</h2><p>لن تُمسح السلة إلا يدويًا.</p><Button type="button" onClick={()=>{clearBasket();router.push("/")}}>إنهاء المعاينة والعودة للرئيسية</Button></section></main>}

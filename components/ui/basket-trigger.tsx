"use client";
import{useRef}from"react";import{BasketIcon}from"./icons";import{useBasket}from"@/components/basket/use-basket";
export function BasketTrigger({compact=false}:{compact?:boolean}){const{items,openDrawer}=useBasket();const triggerRef=useRef<HTMLButtonElement|null>(null);return <button ref={triggerRef} type="button" className={`basket-trigger ${compact?"basket-trigger--compact":""}`} aria-label={`سلة العطاء، ${items.length} عناصر`} aria-haspopup="dialog" onClick={()=>openDrawer(triggerRef.current)}><BasketIcon/>{!compact&&<span>سلة العطاء</span>}<span className="basket-count" aria-hidden="true">{items.length}</span></button>}

import type{StoryMedia}from"@/types/stories";import type{ProjectRecord}from"@/data/projects";export function FeaturedFieldFilm({story,project,onOpen}:{story:StoryMedia;project:ProjectRecord;onOpen?:(id:string)=>void}){return <section id="featured-film" className="stories-section featured-film"><div className="site-container film-layout"><button type="button" className="film-poster" onClick={()=>onOpen?.(story.id)} aria-label={`فتح معاينة ${story.title}`}><span className="play-button" aria-hidden="true">▶</span><strong>{story.sourceLabel}</strong></button><article><span className="status-chip">{story.status==="media-required"?"الوسائط مطلوبة":"قيد التوثيق"}</span><h2>{story.title}</h2><p>{story.summary}</p><dl><div><dt>المنطقة</dt><dd>{project.region}</dd></div><div><dt>المشروع</dt><dd>{project.title.ar}</dd></div><div><dt>المدة</dt><dd>{story.duration??"DURATION REQUIRED"}</dd></div></dl><div className="film-actions"><button type="button" className="button button-primary" onClick={()=>onOpen?.(story.id)}>تشغيل الفيديو</button><a className="button button-secondary" href={`/projects/${project.slug}`}>دعم المشروع</a><a className="text-link" href="/impact#documents">عرض التقرير</a></div></article></div></section>}

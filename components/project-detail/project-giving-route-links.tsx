import {Button} from "@/components/ui/button";import type {ProjectRecord} from "@/data/projects";
export function ProjectGivingRouteLinks({project}:{project:ProjectRecord}){const links=[project.donationTypes.includes("zakat")?{href:"/zakat",label:"استكشف مسار الزكاة"}:null,project.donationTypes.includes("waqf")?{href:"/waqf",label:"استكشف مسار الوقف"}:null,project.donationTypes.includes("recurring")?{href:"/recurring",label:"أنشئ عطاءً مستمرًا"}:null].filter(Boolean) as {href:string;label:string}[];if(!links.length)return null;return <section className="project-giving-route-links" aria-label="مسارات عطاء مرتبطة بالمشروع"><div className="site-container"><div><span>مسارات مرتبطة</span><h2>اختر طريقة عطاء تناسب نيتك</h2></div><div>{links.map(x=><Button key={x.href} href={x.href} variant="outline">{x.label}</Button>)}</div></div></section>}

import Link from"next/link";import type{KnowledgeArticle}from"@/types/knowledge";import{ArticleReviewStatus}from"./article-review-status";export function ArticleCard({article}:{article:KnowledgeArticle}){return <article className="article-card"><ArticleReviewStatus status={article.reviewStatus}/><span>{article.readingMinutes} دقائق قراءة</span><h3><Link href={`/knowledge/${article.slug}`}>{article.title}</Link></h3><p>{article.summary}</p><ul>{article.tags.map(t=><li key={t}>{t}</li>)}</ul></article>}

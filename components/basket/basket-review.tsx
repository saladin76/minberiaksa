"use client";
import{useCallback,useEffect,useMemo,useState}from"react";import{Button}from"@/components/ui/button";import type{BasketItem}from"@/types/basket";import{BasketDeleteDialog}from"./basket-delete-dialog";import{BasketDocuments}from"./basket-documents";import{BasketEditDialog}from"./basket-edit-dialog";import{BasketItemCard}from"./basket-item-card";import{BasketSummary}from"./basket-summary";import{getIntentionCounts}from"./basket-utils";import{useBasket}from"./use-basket";
export function BasketReview(){const{items,hydrated,updateItem,removeItem,restoreItem}=useBasket();const[editItem,setEditItem]=useState<BasketItem|null>(null),[deleteItem,setDeleteItem]=useState<BasketItem|null>(null),[undoItem,setUndoItem]=useState<{item:BasketItem;index:number}|null>(null);const counts=useMemo(()=>getIntentionCounts(items),[items]);useEffect(()=>{if(!undoItem)return;const t=setTimeout(()=>setUndoItem(null),5000);return()=>clearTimeout(t)},[undoItem]);const closeEdit=useCallback(()=>setEditItem(null),[]),closeDelete=useCallback(()=>setDeleteItem(null),[]);const confirmDelete=()=>{if(!deleteItem)return;const index=items.findIndex(i=>i.id===deleteItem.id);removeItem(deleteItem.id);setUndoItem({item:deleteItem,index});setDeleteItem(null)};if(!hydrated)return <div role="status">جارٍ استعادة سلة العطاء التجريبية…</div>;if(!items.length)return <section className="basket-empty-page"><h1>سلة عطائك فارغة</h1><p>ابدأ باختيار مشروع أو زكاة أو وقف أو خطة عطاء مستمر.</p><div className="basket-empty-actions"><Button href="/projects">استكشف المشاريع</Button><Button href="/zakat" variant="outline">احسب زكاتك</Button><Button href="/waqf" variant="outline">أنشئ وقفًا</Button><Button href="/recurring" variant="outline">فعّل عطاءً مستمرًا</Button></div></section>;const due=items.filter(i=>i.intent!=="recurring"&&i.donationMode!=="recurring").reduce((s,i)=>s+i.amount,0);return <><section className="basket-hero"><span>الخطوة 1 من 4</span><h1>راجع سلة عطائك</h1><p>تأكد من المشروع والنية والمبلغ والتفاصيل المرتبطة بكل مساهمة قبل المتابعة.</p><ol className="basket-progress"><li aria-current="step">مراجعة السلة</li><li>بيانات المتبرع</li><li>طريقة الدفع</li><li>التأكيد</li></ol></section><div className="basket-layout"><div className="basket-items-column"><section><h2>نيات ومساهمات السلة</h2><p>لا يتم دمج النيات أو أصحاب الأوقاف أو الخطط المختلفة.</p>{items.map(i=><BasketItemCard key={i.id} item={i} onEdit={()=>setEditItem(i)} onDelete={()=>setDeleteItem(i)}/>)}</section><section className="basket-intentions"><h2>نياتك في هذه السلة</h2>{Object.entries(counts).map(([label,count])=><p key={label}><strong>{label}</strong>: {count}</p>)}<small>كل نية ستبقى منفصلة داخل السجل والإيصال والتوثيق.</small></section><BasketDocuments items={items}/><section className="basket-trust"><h2>الخصوصية والثقة</h2><p>يُحفظ محتوى السلة غير الشخصي داخل sessionStorage لهذه الجلسة فقط. لا تُحفظ بيانات المتبرع أو البطاقة أو مدخلات حاسبة الزكاة.</p></section></div><div><BasketSummary items={items}/><div className="basket-page-actions"><Button href="/checkout" fullWidth>المتابعة إلى Checkout</Button><Button href="/projects" variant="outline" fullWidth>إضافة مشروع آخر</Button></div></div></div><div className="basket-mobile-bar"><div><span>الدفع لمرة واحدة</span><strong>{due.toLocaleString("en-US")} USD</strong></div><Button href="/checkout">المتابعة</Button></div>{editItem&&<BasketEditDialog item={editItem} onClose={closeEdit} onSave={u=>{updateItem(editItem.id,u);setEditItem(null)}}/>}{deleteItem&&<BasketDeleteDialog title={deleteItem.projectTitle} onCancel={closeDelete} onConfirm={confirmDelete}/>} {undoItem&&<div className="basket-undo-toast" role="status" aria-live="polite"><span>تمت إزالة العنصر</span><button type="button" onClick={()=>{restoreItem(undoItem.item,undoItem.index);setUndoItem(null)}}>تراجع</button></div>}</>}

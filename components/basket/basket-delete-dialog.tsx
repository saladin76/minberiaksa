"use client";
import{useEffect,useRef}from"react";import{Button}from"@/components/ui/button";
export function BasketDeleteDialog({title,onCancel,onConfirm}:{title:string;onCancel:()=>void;onConfirm:()=>void}){const dialogRef=useRef<HTMLDivElement|null>(null),cancelRef=useRef<HTMLButtonElement|null>(null);useEffect(()=>{const previous=document.activeElement instanceof HTMLElement?document.activeElement:null,old=document.body.style.overflow;document.body.style.overflow="hidden";requestAnimationFrame(()=>cancelRef.current?.focus());const key=(event:KeyboardEvent)=>{if(event.key==="Escape"){event.preventDefault();onCancel();return}if(event.key!=="Tab"||!dialogRef.current)return;const elements=Array.from(dialogRef.current.querySelectorAll<HTMLElement>('button:not([disabled]),[tabindex]:not([tabindex="-1"])'));if(!elements.length)return;const first=elements[0],last=elements[elements.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}};document.addEventListener("keydown",key);return()=>{document.body.style.overflow=old;document.removeEventListener("keydown",key);requestAnimationFrame(()=>previous?.focus())}},[onCancel]);return <div className="basket-modal-layer"><button className="basket-modal-backdrop" type="button" aria-label="إلغاء الإزالة" onClick={onCancel}/><div ref={dialogRef} className="basket-delete-dialog basket-modal-dialog" role="alertdialog" aria-modal="true" aria-labelledby="basket-delete-title" aria-describedby="basket-delete-description"><h2 id="basket-delete-title">إزالة هذا العنصر من سلة العطاء؟</h2><p id="basket-delete-description">سيتم حذف «{title}» من هذه الجلسة التجريبية، ويمكن التراجع فورًا بعد الحذف.</p><div className="basket-modal-actions"><button ref={cancelRef} type="button" className="ui-button ui-button--outline ui-button--medium" onClick={onCancel}><span>إلغاء</span></button><Button type="button" variant="danger" onClick={onConfirm}>إزالة</Button></div></div></div>}

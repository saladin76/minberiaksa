import { footerNavigation } from "@/config/navigation";
import { siteConfig } from "@/config/site";
import { BrandMark } from "@/components/ui/brand-mark";
import { Container } from "@/components/ui/container";
import { CurrencySelector } from "@/components/ui/currency-selector";
import { LanguageSelector } from "@/components/ui/language-selector";
export function SiteFooter(){const year=new Date().getFullYear();return <footer className="site-footer" id="contact"><Container><div className="footer-main-grid"><div className="footer-institution-column"><div className="footer-brand-column"><BrandMark inverse/><h2>{siteConfig.nameAr}</h2><p>{siteConfig.description}</p></div><section className="footer-contact-block"><h3>التواصل</h3><address><a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a><div className="footer-contact-phones">{siteConfig.phones.map(phone=><a dir="ltr" key={phone} href={`tel:${phone.replace(/\s/g,"")}`}>{phone}</a>)}</div><span dir="ltr">{siteConfig.address}</span></address></section></div><div className="footer-navigation-grid">{footerNavigation.map(column=><details className="footer-link-column" key={column.title}><summary>{column.title}</summary><nav aria-label={column.title}>{column.links.filter(item=>!item.href.startsWith("/account")&&!item.href.startsWith("/checkout")).map(item=><a key={item.label} href={item.href}>{item.label}</a>)}</nav></details>)}</div></div><div className="footer-controls-row"><p>اللغة والعملة</p><div className="footer-selectors"><LanguageSelector compact/><CurrencySelector compact/></div></div><div className="footer-bottom-row"><p>© {year} {siteConfig.nameAr}. جميع الحقوق محفوظة.</p></div></Container></footer>}
